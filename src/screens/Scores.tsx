// eslint-disable-next-line no-use-before-define
import React from 'react';
import LoadingSpinner from '../components/LoadingSpinner';

const Scores = () => <LoadingSpinner />;

export default Scores;
